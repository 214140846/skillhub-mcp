作者:Codex|创建时间:2026-01-18 10:00

# 方案概述
目标是将项目中的品牌名称与 GitHub 账号统一替换：
- “Skillz” 统一替换为 “Skillhub MCP”
- “intellectronica” 统一替换为 “214140846”

该改动属于全局品牌重命名，需覆盖文档、配置、可执行入口描述与日志文本，确保对外展示、使用说明、以及运行时提示一致。

# 需求清单
- [ ] 全文统一替换 “Skillz” 为 “Skillhub MCP”
- [ ] 全文统一替换 “intellectronica” 为 “214140846”
- [ ] 更新 README 中的安装、运行、链接与示例文本
- [ ] 更新配置与元数据（如包描述、主页、Issue 链接）
- [ ] 更新运行提示与 CLI 描述文本，确保用户看到的新品牌一致

# 影响范围与改动点
- 文档：README.md
- 配置：pyproject.toml
- 运行与描述文本：src/skillz/__init__.py、src/skillz/__main__.py、src/skillz/_server.py、src/skillz/_version.py
- 镜像与启动说明：Dockerfile、README 中的镜像名称
- 版权与作者信息：LICENSE 中的域名

# 方案流程
```mermaid
flowchart TD
  A 开始
  B 收集替换范围
  C 更新文档与配置
  D 更新运行提示文本
  E 校验一致性
  F 完成
  A --> B --> C --> D --> E --> F
```

# 待确认事项
- “intellectronica” 在邮箱与域名中的替换规则是否为原样替换（例如 eleanor@214140846.net）(需用户确认)
- Docker 镜像名与 GitHub 链接是否完全按新账号替换（例如 214140846/skillz）(需用户确认)
- 是否需要同时将项目展示名与 CLI help 中的 “Skillz” 改为 “Skillhub MCP”，保持对外一致性 (需用户确认)

# 风险与回滚
- 风险：外部链接与镜像名变更可能导致旧文档指向失效
- 回滚：保留旧名称文本或在 README 中增加兼容说明（如有需要）

# 说明
该方案面向单人开发执行，目标是一次性完成全局替换，避免残留不一致文本。
